# Configurações da API1

spring:
  application:
    name: api1                          # Nome da aplicação
  cloud:
    config:
      uri: http://localhost:9090        # Endereço do Config Server
      failFast: true                    # Como lidar com uma configuração não encontrada (Ex.: Sem arquivo de propriedades)