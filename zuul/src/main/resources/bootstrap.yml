#Arquivo de configuração do Gateway Netflix Zuul

spring:
  application:
    name: zuul
server:
  port: 8080
  
eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:9091/eureka/
  instance:
    hostname: localhost
  
